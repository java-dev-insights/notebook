echo =========================================================
echo "git fetch --all -p; git pull; git status;"
echo =========================================================
git fetch --all -p; git pull; git status;
